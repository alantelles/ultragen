class Tops

Tops.ret = function()
    return false
end

function myTyped(zika, times, show=Tops.ret())
    if (show)
        print("got to show")
    else
        print("no show")
    end
    for(times, i)
        print(_i, ': ', zika)
    end
end

function superJoin(List l)
    ret = ' super '.join(l)
    return ret
end

m = ["ui", "ai", "ei", "oi"]
print(superJoin("zika"))
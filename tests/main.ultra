decorator Decor(a, b, fn)
    print(a, b)
    fn(*$funcArgs)
end

m = Decor('a', 'b', function ()*
    $funcArgs.each(function(i, _i, x)
        print(i)
    end)
end)

m(*['tika', 'doi'])
m(*range(5))
function show(params)
    live 'showing post: ' + (str(params))
end

function update(params)
    live 'updating post: ' + str(params['id'])
end 

function edit(params)
    live 'editando post: ' + str(params['id'])
end

function create(params)
    live 'criando novo post'
end

function save(params)
    live 'salvando novo post'
end

function list(params)
    live 'listando todos os posts'
end

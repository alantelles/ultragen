nome = 'Alan'
email = 'alan@gomes.com'
pass = '123456'
songs = ['michael', 'vedo', 'erykah badu']

live "nome: " + nome
live "email: " + email
live "pass: " + pass

function a(songs)
    live "songs: ["
    for (songs, i)
        if (_i != int(songs.length()) - 1)
            live str(i) + ', '
        else
            live str(i)
        end
    end
    live "]"
end

print(a(songs))

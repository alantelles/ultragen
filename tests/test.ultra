include @Helpers

route = $request['route'].
    sub(1, $request['route'].
    length())

function taDoido()
    live 'ta doido?'
end 

routes = {            
    'posts/mah': 'mostra novo post',
    'posts/:int.id': 'mostra post por id',
    'posts/:str.id': 'mostra por nome',
    'users': 'listar usuarios',
    null : taDoido()
}

live responseRoute(route, $request['method'], routes)

live '<br>'
live 'rota: ' + ($request['route'])
include @Core.Brook.AppResponse
include @Core.DateTime
include @Core.Web.Cookie
include @Core.DateTime

route = $request[:route]
if (route == '/redir')
    AppResponse.redirect("/novidade")
end
live "<a href='/redir'>Redir</a>"
live $request
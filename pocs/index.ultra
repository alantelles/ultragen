init: router, template, page
ext = '.ultra.html'
route = substring($_route, 1)
if: eq(route, '')
    route = 'form'
endIf
template = concat(route, ext)
include: template, page
include: 'base.ultra.html'
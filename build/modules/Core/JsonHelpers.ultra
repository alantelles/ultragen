function dumpObj(data)
    ret = '{'
    x = []
    for (data.keys(), d)
        x.append('"' + d + '": ' + dumpVal(data[d]))
    end
    ret += ','.join(x)
    ret += '}'
    return ret
end

function dumpValList(data)
    ret = '['
    x = []
    for (data, d)
        x.append(dumpVal(d))
    end
    ret += ','.join(x)
    ret += ']'
    return ret
end

function dumpVal(data)
    input_type = typeof(data)
    # tri = ['TIntegerInstance', 'TBooleanInstance', 'TFloatInstance']
    tri = [Integer.class, Boolean.class, Float.class]
    if ((tri.pos(input_type)) > -1)
        return str(data).lower()
    elsif (input_type == (String.class))
        return '"' + data + '"'
    elsif (input_type == (Dict.class))
        return dumpObj(data)
    elsif (input_type == (List.class))
        return dumpValList(data)
    else
        return 'null'
    end
end